services:
  - type: web
    name: wanderwise-api
    runtime: node
    plan: free
    buildCommand: "npm install"
    startCommand: "node server/server.js"
    envVars:
      - key: GEMINI_API_KEY
        sync: false